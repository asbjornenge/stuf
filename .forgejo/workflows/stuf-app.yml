name: stuf-app

on:
  push:
    tags:
      - "production-*.*.*"
      - "app-*.*.*"

jobs:
  build-and-deploy:
    runs-on: docker
    uses: asbjornenge/forgejo-ci/.forgejo/workflows/stuf-app.yml@main
    secrets: inherit
